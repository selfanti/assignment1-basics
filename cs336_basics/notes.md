# BPE分词分词组件实现

1. 预分词，按照输入的文本按照空格划分，注意到windows和linux的换行符是不一样的，应该统一使用CRLF
2. 训练BBPE算法，测试中没有使用GPT的remapping
原版GPT2的实现时将0到255之中不可见的字符和空格全部加上一个偏移量256，变成在unicode中可见的字符
3. 编解码的实现
BBPE算法可以编解码任何unicode字符，面对vocabulary中没有的字符时，该字符的utf-8编码按照字节被分开进行编码，因为0到255所有的字符都在字符集中，所以一定可以完成编码.
